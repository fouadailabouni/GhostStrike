# GhostStrike v3.0.0 [PHANTOM] — Python Dependencies
# © 2026 Fouad Ailabouni. All Rights Reserved.

# ── GUI Framework ──
customtkinter>=5.2.0
Pillow>=10.0.0

# ── Framework Libraries (used by lib/*.sh Python helpers & GUI) ──
requests>=2.31.0          # HTTP client for notifications, CVE lookup, webhooks
jsonschema>=4.21.0        # finding.schema.json validation (lib/finding_ontology.sh)
PyYAML>=6.0.1             # policy.yaml, scope.yml, benchmark scenario parsing
rich>=13.7.0              # Terminal progress bars, tables (metrics/reproducibility_report.sh)
cryptography>=42.0.0      # Credential vault AES-256-GCM encryption

# ── Reporting ──
jinja2>=3.1.3             # HTML report templating
weasyprint>=62.0          # HTML → PDF conversion (optional, install separately if needed)
markdown>=3.5.2           # Markdown report rendering
python-docx>=1.1.0        # Report Studio DOCX export (report_studio/exporters.py)
fpdf2>=2.7.0              # Pure-Python PDF fallback when WeasyPrint's native libs aren't available

# ── Utility ──
python-dateutil>=2.9.0    # Date parsing for engagement timelines
tabulate>=0.9.0           # Table formatting for CLI reports
click>=8.1.7              # CLI argument parsing for standalone Python scripts

# ── AI Co-Pilot (ai_engine/) ──
# Only needed if you use AI Co-Pilot mode in the GUI; the app itself starts
# fine without these (the toggle greys out with a clear reason instead of
# the app failing to launch). API keys are read from lib/vault.sh first,
# falling back to ANTHROPIC_API_KEY / OPENAI_API_KEY -- see model_provider.py.
anthropic>=0.40.0         # Claude backend (default)
openai>=1.0.0             # GPT-4o fallback backend

# ── MCP Server (ai_engine/mcp_server/) ──
# Local, stdio-only -- exposes one engagement's findings/attack-graph/repro
# data to an MCP client (Claude Code, Claude Desktop, Cursor). No network
# transport, no listening socket. Pinned >=2.0.0: the 1.x decorator API
# (@server.list_tools()) is NOT compatible with 2.x's MCPServer/@server.tool()
# API this code is written against -- see ai_engine/mcp_server/server.py.
mcp>=2.0.0

# ── Build / Packaging ──
pyinstaller>=6.0.0        # Windows .exe build